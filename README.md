## Build and Run

### Analysis Engine (Rust)

cd /path/to/rust_engine && cargo build --release

### 

cd /path/to/csharp_ui && dotnet run

alias build-all='cd /path/to/rust_engine && cargo build --release && cd /path/to/csharp_ui && dotnet run'